// var widgetBar = document.getElementsByClassName('dos');
// widgetBar.innerHTML = "sff"

console.log("testtt")

if (1) {
    //Creating Elements
    var btn = document.createElement("BUTTON")
    var t = document.createTextNode("CLICK ME");
    btn.appendChild(t);
    //Appending to DOM 
    document.body.appendChild(btn);
}